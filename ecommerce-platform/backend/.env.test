NODE_ENV=test
PORT=5001 # Different port for test server if needed, though supertest often uses the app directly

# MongoDB Connection String for Testing
# IMPORTANT: Use a dedicated test database that can be wiped or reset.
MONGO_URI=mongodb://localhost:27017/ecommerce_platform_test

# JWT Secret Key (can be the same as dev or different)
JWT_SECRET=your_jwt_secret_key_here_for_testing
JWT_EXPIRES_IN=1h
JWT_COOKIE_EXPIRES_IN=1
